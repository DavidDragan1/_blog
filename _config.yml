title: Code & Contemplations
author: David Dragan
email: daviddragan377@@gmail.com
url: daviddragan1.github.io/_blog/ # root address of the site
baseurl: # subpath of the site, e.g. "/blog" (leave it blank "" if site shouldn't use a subpath)
description: >
  Welcome, this blog is an outlet of my musings and programming projects.

permalink: /:slug.html

favicon: "logo.png" # name+extension of favicon (which must be put on the root folder)
# goat_counter: "yoursitename" # put your GoatCounter name if you want to use GoatCounter analytics

remote_theme: riggraz/no-style-please # if you are using GitHub Pages, change it to remote_theme: riggraz/no-style-please

theme_config:
  appearance: "auto" # can be "light", "dark" or "auto"
  back_home_text: "\.." # customize text for homepage link in post layout
  date_format: "%Y-%m-%d" # customize how date is formatted
  show_description: false # show blog description in home page

sass:
  style: :compressed

plugins:
  - jekyll-feed
  - jekyll-seo-tag
